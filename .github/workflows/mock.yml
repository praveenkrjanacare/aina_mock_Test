name: Run Appium Tests on Emulator

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    name: Appium Test on Android Emulator
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v3

      - name: Set up Java
        uses: actions/setup-java@v3
        with:
          distribution: 'temurin'
          java-version: '17'  # change if needed

      - name: Install Maven
        run: sudo apt-get install -y maven

      - name: Start Android Emulator
        uses: reactivecircus/android-emulator-runner@v2
        with:
          api-level: 30
          target: google_apis
          arch: x86_64
          avd-name: Pixel_8_API_30  # Default emulator name
          force-avd-creation: true
          emulator-options: "-no-window -no-audio"
          disable-animations: true

      - name: Wait for Emulator to Fully Boot
        run: |
          echo "Waiting for emulator to stabilize..."
          adb wait-for-device
          sleep 60  # Wait for 60 seconds to ensure device is ready

      - name: Run Appium Tests
        run: mvn clean test -Dsurefire.suiteXmlFiles=testng.xml

      - name: Upload Test Report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: test-results
          path: target/surefire-reports/
